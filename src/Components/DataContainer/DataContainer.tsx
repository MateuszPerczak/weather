import styled from "@emotion/styled";
import type DataContainerComponent from "./DataContainer.types";

const DataContainer: DataContainerComponent = styled.div`
  display: flex;
  gap: 10px;
  flex-wrap: wrap;
`;

export default DataContainer;
